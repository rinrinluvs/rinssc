function setup() {
    createCanvas(200, 200);
    background(255)
}

function draw() {
    // Zoog demon
    // background(255)
    // ellipseMode(CENTER)
    // rectMode(CENTER)

    // stroke(0)
    // fill(175)
    // rect(mouseX, mouseY, 20, 100)

    // stroke(0)
    // fill(255)
    // ellipse(mouseX, mouseY-30, 60, 60)

    // fill(0)
    // ellipse(81, 70, 16, 32)
    // ellipse(119, 70, 16, 32)

    // stroke(0)
    // line(90, 150, 80, 160)
    // line(110, 150, 120, 160)


    // Line drawing

    // stroke(0)
    // strokeWeight(Math.abs(mouseX -pmouseX))
    // line(pmouseX, pmouseY, mouseX, mouseY)
    
}